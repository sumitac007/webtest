name: Merge Workflow

on:
  push:
    branches:
      - q1a

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      # Add your build and deployment steps here
      # This is just a placeholder example step
      
      - name: Build and deploy
        run: |
          echo "Build and deploy script"
